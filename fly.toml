app = "quiz-app-zhujiafeng"
primary_region = "hkg"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  min_machines_running = 1

[mounts]
  source = "quiz_data"
  destination = "/app/backend/data"
